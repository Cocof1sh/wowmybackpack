├── week1
│   ├── code
│   │   ├── 1        # 签到题
│   │   └── 2        # HelloWorld
│   └── video        # 演示视频
├── week2
│   ├── code
│   │   ├── 1        # GPIO缺省配置
│   └── └── 2        # 软件模拟I2C（包含三个文件，分别是一个多写的rm.h和两个必要的.h和.c文件）
├── week3
│   ├── code
│   │   ├── 1        # 代码main.c在core的src里面（用的ide+MX），.h文件都在Inc
│   └── └── 2        # 视频提交在提交表格附件里面（前面的按钮是直接控制小灯的，后面的按钮是切换模式的，也就是视频中的两种小灯模式）
├── week4
│   ├── code          （这个任务我分成了两个项目来做，分别是exti和tim）
│   │   ├── 1        # EXTI的对应task_EXTI文件夹, main.c在core的src，.h文件在Inc（用的ide+MX）   ！！！现象是按下按钮触发外部中断：闪亮300ms然后熄灭
│   │   ├── 2        # TIM的对应task_TIM文件夹, main.c在core的src，.h文件在Inc（用的ide+MX）  ！！这个是三种不同频率的小灯闪烁各10s
│   │   ├── 3        # 视频提交在提交表格附件，也是分开的独立两个工程的视频
│   └── └── 4        # 我又做了一个新的TIM任务叫new巴拉巴拉，也保留了之前第一次的程序，也是算和之前的自己对比了（其实也是因为之前的好像做的不好，视频同样在表格里噢
├── week5
│   ├── code
│   │   ├── 1        # 代码main.c在core的src里面（用的keil+MX），.h文件都在Inc
│   └── └── 2        # 视频提交在提交表格附件里面 按按键控制转速
├── week6
│   ├── code
│   │   ├── 1        # 代码main.c在core的src里面（用的keil+MX），.h文件都在Inc
│   └── └── 2        # 视频提交在提交表格里面 按按键控制的
│
└──IIC的程序已经更改
│
└──小车的代码是那个car的，因为之前有一个想好的标准库框架，然后就用标准了，后面推翻重做基本上全写main里面了哈哈。
